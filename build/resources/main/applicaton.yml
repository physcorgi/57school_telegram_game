spring:
  application:
    name: demo
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/telegram_bot_db
    username: myuser
    password: mypassword
  jpa:
    show-sql: true